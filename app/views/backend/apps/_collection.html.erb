<div id="main_list">
  <h4 class="lead well">
    <%=resource_class.model_name.human %>列表
  </h4>
  <ul class="nav nav-list">
  <!%-- erb渲染页面性能比较差，改为字符串拼接。--%>
    <%=raw collection.map{ |item| %Q[<li id="app_#{item.id}" class="well">
      <span>
      #{h item.name}
      <a href="/backend/apps/#{item.id}/acls" data-remote="true">权限设定</a>
      </span>
      <div class="form" style="display:none"></div>
    </li>]
    }.join  %>
  </ul>
</div>
